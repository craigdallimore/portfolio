App.module('View', function(View, App, Backbone, Marionette, $, _) {

    View.CMS = Backbone.View.extend({
        initialize: function() {
            _.log('Init CMS view');
        }
    });

});
